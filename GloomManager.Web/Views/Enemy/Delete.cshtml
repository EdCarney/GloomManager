@model GloomManager.Web.Models.EnemyFormViewModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    @Html.AntiForgeryToken()
    <h4>@Html.DisplayFor(model => model.Enemy.Name)</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Enemy.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Enemy.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Enemy.Level)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Enemy.Level)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Enemy.Eliteness)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Enemy.Eliteness)
        </dd>
    </dl>

    <partial name="_StatDetails" model="Model" />

    <br />

    <form asp-action="Delete">
        <input type="hidden" asp-for="Enemy.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a class="btn btn-info" asp-action="FullIndex">Back to List</a>
    </form>
</div>
